package hashmap;

public class HashCodeExample {
    static void main() {
        String name = "kaushal";
        Integer pin = 89491;

        System.out.println("Hashcode of name " + name + " is " + name.hashCode());
        System.out.println("Hashcode of pin "+ pin + " is " + pin.hashCode());
    }
}
